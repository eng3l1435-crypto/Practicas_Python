[project]
name = "atlas-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "angel_hernandez", email = "eng3l1435@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pymongo>=4.18.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
atlas-py = "atlas_py:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
